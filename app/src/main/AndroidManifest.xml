<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.example.my1">

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name=".login">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".MainActivity" android:label="@string/app_name" />
		<activity android:name=".NewProMarketActivity" android:label="@string/app_name" />
		<activity android:name=".BoyShoesActivity" android:label="@string/app_name"/>
		<activity android:name=".GirlShoesActivity" android:label="@string/app_name"/>
		<activity android:name=".MoreShoesActivity" android:label="@string/app_name"/>
		<activity android:name=".CommonActivity" android:label="@string/app_name"/>
		<activity android:name=".test" android:label="My1"/>
		<activity android:name=".MyFr4" android:label="@string/app_name"/>
		<activity android:name=".db.shop" android:label="My1"/>
		<activity android:name=".MyFrament4" android:label="My1"/>
	</application>

</manifest>